[include]
    path = ~/.gitconfig.local
[diff]
    tool = vimdiff
    indentHeuristic = true
[difftool]
    prompt = false
[merge]
    tool = vimdiff
[mergetool]
    prompt = false
[alias]
    undo = "!f() { git reset --hard $(git rev-parse --abbrev-ref HEAD)@{${1-1}}; }; f"
[ghq]
    root = ~/ghq
